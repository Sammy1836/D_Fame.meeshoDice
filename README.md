After cloning the project, follow the following code sequence:
```
cd frontend
npm install
npm run dev
```

Open a new terminal and run the following code:
```
cd ../backend
npm install
node server.js
```
